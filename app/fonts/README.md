# Local Fonts

These WOFF2 files are vendored from the previous remote font setup so production
builds do not need network access to fetch font assets. `app/layout.tsx` loads
them through `next/font/local`.
